<!--pages/home/home.wxml-->

<!-- 小程序的数据绑定 {{}}Mustache-->
<view>Hello {{name}}</view>
<view>当前时间 {{age}}</view>

<!-- 列表展示:wx:for -->
<view wx:for="{{animalArr}}">{{index}}:{{item.name}}-{{item.age}}</view>

<!-- 事件监听改变data -->
<view>当前计数:{{count}}</view>
<button size="mini" bindtap="addHandleBtnClick">++</button>
<button size="mini" bindtap="reduceHandleBtnClick">--</button>